[project]
name = "orm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.7",
]

[dependency-groups]
dev = [
    "faker>=37.0.0",
    "ipython>=9.0.2",
    "ruff>=0.9.10",
]
